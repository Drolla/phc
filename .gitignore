# Python bytecode / cache
__pycache__/
*.py[cod]
*$py.class

# Packaging
*.egg-info/
build/
dist/

# Virtual environments
.venv/
venv/
env/

# Test / tooling caches
.pytest_cache/
.benchmarks/
.mypy_cache/
.ruff_cache/

# Editors / OS
.DS_Store
.idea/
.vscode/

# Generated logs (e.g. logdb CSV output, or a log: file destination
# written next to whichever system YAML configured it)
logs/
*.log

# Generated recovery state (extensions.recovery's persisted endpoint
# values -- e.g. examples/recovery_system.yaml's and
# examples/full_house_system.yaml's own state/*.yaml)
state/

# Claude Code local settings
.claude/settings.local.json
CLAUDE.md

# Personal home automation config (may become a symlink to a backed-up
# location outside the repo) -- never commit, see CLAUDE.md.
fusion18/
