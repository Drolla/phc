# Skills

Empty for now. Once this project defines a specialized, reusable workflow
(e.g. a Claude Code skill for a project-specific job), add one Markdown
file per skill here describing what it does and when it applies, and list
it in `../index.json` under `skills` — so every supported coding-assistant
tool can discover and follow it, not just the tool that can execute it
natively.
