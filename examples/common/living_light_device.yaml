# Assumes the including system YAML defines an "intervals: { fast: ... }" entry.
id: living_light
module: virtual
name: Living Room Light
update: fast
endpoints:
  - key: state
    writable: true
    type: int
    values: { 0: "off", 1: "on" }
    default: 0
